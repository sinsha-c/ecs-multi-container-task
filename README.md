# ecs-multi-container-task
Multi-container ECS Fargate deployment (apache + busybox sidecar) behind an ALB, with secrets securely injected via AWS Secrets Manager and IAM Task/Execution Roles — no hardcoded credentials.
